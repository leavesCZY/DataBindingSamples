<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="leavesc.hello.databindingsamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="leavesc.hello.databindingsamples.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="leavesc.hello.databindingsamples.Main2Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main3Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main4Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main5Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main6Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main7Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main8Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main9Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main10Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main11Activity" />
        <activity android:name="leavesc.hello.databindingsamples.Main12Activity" />
    </application>

</manifest>